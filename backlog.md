# BrandGuard AI: Backlog

This file is a local convenience mirror of the Notion Kanban backlog. The Notion Kanban page is the source of truth (DNA §0). When the two diverge, Notion wins.

**Notion database:** [BrandGuard AI Version Control](https://www.notion.so/8bdb8b20297648708105838f05d56049)

---

## Future (post-v1.0)

These items come from Kanban v1.2 backlog. None of them are in scope for v1.0.

### B1: LinkedIn launch post

A LinkedIn announcement post timed to the v1.0 release. Disclose the upstream `intelliflow-core` v2 lineage in the post body (consistent with README policy). Out of scope for the build sessions; copy and timing are a separate decision.

**Status:** Future. Not for v1.0.

### B2: Audience refinement loop

A second-pass audience editor that lets a marketing user adjust a generated audience definition (add/remove records, broaden/narrow predicates) and re-run copy generation against the adjusted audience. The Audience Discovery Agent in v1.0 returns a fixed audience definition; refinement is a future capability.

**Status:** Future. Not for v1.0.

### B3: Multi-brand support

Support more than one brand voice document, with a routing layer that selects the right voice for the active campaign. v1.0 is single-brand only (single voice doc, single product line). Multi-brand requires per-brand approval policies and brand-isolation tenancy that are out of scope for v1.0.

**Status:** Future. Not for v1.0.

### B4: API wrapper / hosted service

A REST or gRPC API surface around the four native components, suitable for integration with marketing automation tools. v1.0 ships as a library and CLI only.

**Status:** Future. Not for v1.0.
